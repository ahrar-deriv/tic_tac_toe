// x for player1
// o for system or player2

enum Tile { empty, x, o }

enum GameMode { twoPlayerOffline, simpleOffline, difficultOffline, online }

// player2 also could be consider as system
enum GameRole { player1, player2 }
